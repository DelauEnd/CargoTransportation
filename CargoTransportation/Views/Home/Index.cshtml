@{
    ViewData["Title"] = "Home Page";
}

    <div class="text-center">
        <h1 class="display-4">Welcome</h1>
        @{ 
            if (!request.HttpClient.Authenticated)
            {
                <p><input style="width:200px" type="button" value="Вход" onclick="@("window.location.href='" + @Url.Action("login", "authentication") + "'");" /></p>
                <p><input style="width:200px" type="button" value="Регистрация" onclick="@("window.location.href='" + @Url.Action("registration", "authentication") + "'");" /></p>
            }
            else
            {

            }
        }
    </div>
